# Web Frameworks
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-CORS==4.0.0

# Database
SQLAlchemy==2.0.23
psycopg2-binary==2.9.9

# Database Tools & Management
Flask-Admin==1.6.1
sqlalchemy-utils==0.41.1

# Redis
redis==5.0.1

# Authentication & Security
Flask-Login==0.6.3
Flask-JWT-Extended==4.6.0
PyJWT==2.8.0
bcrypt==4.1.2
passlib[bcrypt]==1.7.4

# AI & ML - Railway compatible (CPU-only)
# Usamos ruedas CPU para evitar dependencias CUDA en Railway
# ACTUALIZACIÓN Nov 2025: Migrado a CLIP ViT-L/14 (768D, ~3GB RAM)
# Railway Pro Plan requerido: $20/mes (8GB RAM recomendado)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.1+cpu
torchvision==0.16.1+cpu
transformers==4.45.0
huggingface-hub>=0.23.2
Pillow==10.1.0
numpy==1.25.2
sentence-transformers>=2.3.0
scikit-learn==1.3.2  # Para matching semántico en LLM query normalizer

# Image Processing & Storage
cloudinary==1.36.0  # Cloudinary para almacenamiento en la nube
python-multipart==0.0.6
python-slugify==8.0.4  # Para slugs de URLs
googletrans==4.0.0rc1  # Para traducción automática en API

# API & Web
pydantic==2.5.0
requests==2.31.0
python-dotenv==1.0.0
certifi>=2023.7.22  # Certificados SSL para descargas de modelos

# Development & Validation
pytest==7.4.3

# GPT-4 Vision Integration (Nov 2025)
openai>=1.3.0  # Cliente oficial OpenAI para GPT-4o Vision

# Otras dependencias del sistema
gunicorn==21.2.0  # WSGI server para producción
Werkzeug==3.0.1  # Utilidades WSGI (incluido con Flask pero fijamos versión)
