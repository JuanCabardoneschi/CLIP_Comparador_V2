# Web Frameworks
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Flask-CORS==4.0.0

# Database
SQLAlchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9

# Database Tools & Management
Flask-Admin==1.6.1
# sqlite-utils==3.35  # REMOVIDO - Solo PostgreSQL soportado
sqlalchemy-utils==0.41.1

# Redis
redis==5.0.1

# Authentication & Security
Flask-Login==0.6.3
Flask-JWT-Extended==4.6.0
PyJWT==2.8.0
bcrypt==4.1.2
passlib[bcrypt]==1.7.4

# AI & ML - Railway compatible (CPU-only)
# Usamos ruedas CPU para evitar dependencias CUDA en Railway
# ACTUALIZACIÓN Nov 2025: Migrado a CLIP ViT-L/14 (768D, ~3GB RAM)
# Railway Pro Plan requerido: $20/mes (8GB RAM recomendado)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.1+cpu
torchvision==0.16.1+cpu
transformers==4.35.2
huggingface-hub==0.19.4
Pillow==10.1.0
numpy==1.25.2
sentence-transformers==2.2.2
scikit-learn==1.3.2  # Para matching semántico en LLM query normalizer

# Visual Detection / Grounding (pruebas)
groundingdino-py==0.1.2  # Wrapper simplificado Grounding DINO (detección zero-shot)
opencv-python==4.9.0.80  # Requerido por groundingdino
pyyaml==6.0.1  # Config parsing para modelos grounding
timm==0.9.12  # Backbones vision (SwinT) usados por Grounding DINO

# Image Processing & Storage
cloudinary==1.36.0  # Cloudinary para almacenamiento en la nube
python-multipart==0.0.6

# API & Web
pydantic==2.5.0
requests==2.31.0
python-dotenv==1.0.0
certifi>=2023.7.22  # Certificados SSL para descargas de modelos

# Development & Validation
pytest==7.4.3
black==23.11.0
isort==5.12.0
flake8==6.1.0

# Utils - Railway compatible
python-slugify==8.0.1
validators==0.22.0
googletrans==4.0.0rc1
